[package]
name = "docbunker-native-broker"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "DocBunker native-messaging broker: minimal browser-facing binary that validates and hands off a document to the app"

[lints]
workspace = true

[dependencies]
docbunker-renderer-api = { path = "../renderer-api" }
serde = { workspace = true, features = ["derive"] }
serde_json = "1"
tempfile = "3"
tracing = { workspace = true }

[dev-dependencies]
# Only used to exercise the same fixture conditions as the app tests.